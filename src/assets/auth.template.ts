//ADD a folder in src called environments and add a file called auth.ts with the right password and username
export const auth = {
  production: false,
  authUser: 'INSERT_USERNAME',
  authPassword: 'INSERT_PASSWORD'
}
